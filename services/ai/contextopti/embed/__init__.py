"""Chunking and embedding for the semantic RAG baseline (M2).

Planned contents:

* ``chunk.py``  -- file/function-level chunking with token accounting
* ``encode.py`` -- embedding backend behind one interface; TF-IDF or BM25 is an
  acceptable v1 so that the baseline does not block on model downloads

Not implemented yet -- M2.
"""
